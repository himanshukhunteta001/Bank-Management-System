server:
  port: 8085

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/himanshukhunteta001/configuration-server
          clone-on-start: true